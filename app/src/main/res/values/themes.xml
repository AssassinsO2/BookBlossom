<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BookBlossom" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.BookBlossom" parent="Base.Theme.BookBlossom" />

    <style name="Base.Theme.temp" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.temp" parent="Base.Theme.BookBlossom" />

    <style name="CustomDialogTheme" parent="Theme.AppCompat.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="CustomBottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="elevation">0dp</item>
        <item name="android:radius">10dp</item>
        <item name="cornerRadius">30dp</item>
    </style>

    <style name="CustomRecyclerViewScrollbar">
        <item name="android:scrollbarThumbVertical">@drawable/custom_scrollbar_thumb</item>
    </style>

    <style name="Base.Theme.Temporary" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Temporary" parent="Base.Theme.BookBlossom" />

    <style name="Base.Theme.InputLayout" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="Theme.InputLayout" parent="Theme.Material3.DayNight.NoActionBar"/>
</resources>